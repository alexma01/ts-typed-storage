pre-commit:
  parallel: false
  piped: true
  commands:
    lint-check:
      priority: 1
      run: yarn lint
    test:
      priority: 2
      run: yarn test
    build:
      priority: 3
      run: yarn build
